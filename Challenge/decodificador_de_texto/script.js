let codigo = {a: 'ai', e: 'enter', i: 'imes', o: 'ober', u: 'ufat'};


let frase = document.querySelector('textarea');
for (let i = 0; i < frase.length; i++);
    codigo.replaceChildren(frase[i])

    console.log(frase);